containers:
  base:
    setup:
      - !Ubuntu xenial
      - !Install ['ca-certificates']
      - !PipConfig
        dependencies: true
      - !Py3Requirements "requirements.txt"

commands:
  run: !Command
    description: Run VK_Bot
    container: base
    run: [python3, run.py]